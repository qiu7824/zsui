# ZSUI v0.2: Native UI Application Closure

## Goal

ZSUI v0.2 is not a window-creation milestone. Its goal is to make a useful,
independent Rust-first UI framework that can build complete lightweight native
desktop applications from one shared application codebase.

The same Rust `State`, `Msg`, `view`, `update`, theme, layout, widget tree and
business services must run through real Win32, AppKit and Linux backends. Platform
code is limited to host, renderer, input adapter and native-service modules.
Android runtime work is outside this milestone.

The one-line entry point is only the application bootstrap contract:

```rust
fn main() -> zsui::ZsuiResult<()> {
    zsui::native_window("Application")
        .size(960, 640)
        .stateful_view(AppState::default(), view, update)
        .run()
}
```

Opening a native window alone does not complete a platform or this milestone.

## Framework Closure

ZSUI v0.2 must close all of these layers as one system.

### Application runtime

- RAII application and window lifecycle
- typed `State`, `Msg`, `Command`, `WidgetId` and `WindowId`
- deterministic `view` and `update` message flow
- explicit effects, timers, asynchronous task completion and invalidation
- `Result<T, ZsuiError>` failures without public backend pointers

### Layout, widgets and composition

- typed `Dp`, `Px`, `Dpi`, constraints, measurement and arrangement
- row, column, stack, grid, overlay, clipping and responsive constraints
- complete widget states: normal, hover, pressed, focused, selected and disabled
- text, button, icon button, text box, text area, check box, radio button,
  toggle, slider, number box, progress, tabs, menu, dialog, tooltip, toast and
  info bar
- list, scroll view and virtual/paged list with stable focus and scroll anchors
- reusable application compositions: navigation shell, command bar, grouped
  settings cards, setting rows, document shell and action areas

A declaration, catalog entry or empty paint path is not an implemented widget.
An implemented widget has layout, input, state, paint, theme and tests.

### Rendering and visual system

- retained widget tree converted to backend-neutral draw plans
- antialiased text and geometry, clipping, opacity and buffered repaint
- damage/invalidation rules that do not flash during resize or scrolling
- semantic theme tokens for typography, color, spacing, radius and elevation
- light, dark, high-contrast and system-theme reactions
- semantic icons resolved through Segoe Fluent/MDL2, SF Symbols and GTK icon
  themes, with an optional licensed fallback asset set

### Input and accessibility

- pointer, wheel, touchpad and keyboard event conversion
- hit testing, capture, focus order, Tab traversal and keyboard activation
- selection, caret, clipboard editing and undo/redo foundations
- native text input and basic CJK IME composition without crashes or lost text
- DPI/Retina/scale-factor relayout and accessible names, roles and states

### Native desktop services

- menus and context menus
- text clipboard
- open/save file dialogs
- system theme and scale notifications
- tray, global shortcuts, notifications and drag/drop where capabilities permit
- explicit capability results for unsupported or partial platform behavior

### Rust build and AI development experience

- safe public API; backend `unsafe` remains private and narrowly documented
- composition and traits instead of control inheritance or reflection
- no global mutable widget registry
- widgets and heavy native dependencies remain optional Cargo features
- default features stay small; feature combinations have automated checks
- `docs/ai-agent.md` and context packs let an agent load only the relevant
  architecture and files instead of scanning the whole repository
- examples demonstrate framework APIs but do not dictate framework boundaries

### Reloadable UI documents and native preview

- define one versioned, platform-neutral `UiDocument` format for semantic
  component trees, layout constraints, theme tokens, localization keys,
  accessibility metadata and stable node IDs
- keep visual structure separate from typed Rust behavior: document action and
  binding slots are validated against an explicit `State`/`Msg` binding
  manifest rather than dispatched through a global string event bus
- provide a `zsui-uic check` path that rejects unknown components, unavailable
  Cargo features, duplicate IDs, invalid properties, unresolved bindings and
  incompatible schema versions before a document reaches a native host
- provide a prebuilt native Viewer that watches a UI document and reloads
  text, tokens, layout properties and compatible component-tree changes without
  invoking Cargo or restarting the window
- resolve semantic page/content spacing through each platform profile and make
  text wrap, ellipsis, weight and alignment explicit document contracts;
  recursive intrinsic layout must include descendant line boxes, gaps, padding
  and native control minimums before the Viewer paints a frame
- preserve focus, text selection, scroll anchors and compatible control state
  across reload by stable identity; structural changes that cannot preserve a
  state must produce an explicit diagnostic instead of silently routing events
  to the wrong node
- let the same document enter real Win32, AppKit and Linux hosts, and emit a
  deterministic structured handoff plus optional screenshots for AI editing
- compile or embed validated documents for release so file watching, parser
  diagnostics and preview transport remain development-only optional surfaces
  and do not force another process or browser runtime into shipped applications

The existing declarative Rust builders remain supported and may be mixed with
document-backed subtrees through typed component slots. A browser/WASM editor
may later project the same semantic document for fast approximate comparison,
but browser pixels are not Win32, AppKit or Linux completion evidence. A full
drag-and-drop designer, timeline editor, collaboration service and arbitrary
Rust-logic visual programming are outside the v0.2 completion gate.

This addition does not replace or remove any existing v0.2 requirement. The
unfinished native services, IME, accessibility, interaction, feature trimming
and target-proof gates below remain mandatory.

### Performance and product-position gate

- keep four independent release workloads: Minimal, Common, Full Native App
  and Viewer; compare ZSUI, egui, Iced, Slint and Tauri only within the same
  workload and window contract
- measure executable size, startup-to-window, first presented frame, empty and
  full-page RSS, hidden RSS, peak RSS, Private RSS/PSS where the operating
  system exposes them, idle CPU and continuous-repaint CPU
- keep formal applications and `zsui-viewer` as separate artifacts; Viewer
  polling, validation, diagnostics and full document-component coverage must
  not enter formal application builds
- keep `UiDocument` a bounded semantic declaration format; dynamic scripting,
  arbitrary string properties, a browser styling/runtime model and product
  behavior are outside its ownership
- require screenshots for every measured workload and reject a run when the
  loaded page, window size, animation state or process tree is contaminated

The current Windows protocol, results and reproduction command are recorded in
[the UI performance matrix](ui-performance-matrix.md).

## Stable Platform Boundary

The safe Rust desktop boundary includes:

- `DesktopHost`
- `WindowService`
- `ClipboardService`
- `FileDialogService`
- `NativeFileDialogService` target-dispatched safe facade
- `MenuService`
- `ThemeService`
- `TextInputService`

Every operation has an explicit `DesktopCapability`. `Partial` and
`Unsupported` are not runtime success. Raw `HWND`, Objective-C objects, GTK
pointers, drawing handles and platform event-loop types remain private.

Backend selection uses independent optional features:

- `windows-win32`
- `macos-appkit`
- `linux-direct` (default Linux backend)
- `linux-gtk`
- `desktop-native` for target-selected desktop dependencies

## Real Backends

### Windows

Keep the proven Win32 host and buffered drawing path. Complete DPI, IME,
accessibility, menus, clipboard, dialogs, system theme, focus, drag/drop and
resize repaint without regressing background erase suppression, antialiasing,
spacing or scrolling.

### macOS

Use a real `NSApplication`/`NSWindow` host, AppKit event loop, Retina scaling,
`NSMenu`, `NSPasteboard`, `NSOpenPanel`, `NSSavePanel`, text input and SF
Symbols. Winit transport is not AppKit completion evidence.

### Linux

Use a real Wayland/X11 window and native event stream. The default lightweight
host presents ZSUI's self-drawn controls directly, uses Cairo/Pango for
rendering and system text, freedesktop icon themes, native IME events, the
system clipboard and XDG desktop portal dialogs. Validate Wayland and retain
X11 compatibility. `linux-gtk` remains an optional compatibility backend.
Neither backend type enters widget APIs.

This is native-window and native-system integration. It does not mean that
ZSUI controls become GTK widget instances: their state, layout and paint remain
the shared Rust framework implementation, with Linux-specific composition and
style parameters.

## Acceptance Applications

`examples/desktop_native_showcase.rs` is the cross-platform interaction probe.
It uses one application entry point and exercises navigation, command bar,
single-line and multiline text, buttons, toggle, list scrolling, theme,
open/save, copy/paste, native menu, resize, scale and keyboard focus.

The notepad, calculator, settings, workbench and paged-list examples are
secondary acceptance probes. Their application code should become smaller as
framework compositions mature, but demo line count is not the architecture
target and support-project dependencies are not committed.

## Evidence Gate

A capability or backend advances only with all applicable evidence:

1. Contract: safe API, capability semantics and no backend-type leakage.
2. Unit: layout, state machine, message routing, focus, theme and error tests.
3. Compile: formatting, no-default tests, full tests, feature matrix and target
   backend/example checks on Windows, macOS and Ubuntu stable Rust.
4. Runtime: startup, light/dark, text input, menu, dialog and resize captures
   under `docs/platform-proof/<platform>/`.
5. Interaction: lifecycle, typed messages, selection/copy/paste, CJK IME,
   scrolling, Tab focus, open/save, theme change and artifact-free resize.
6. Authoring: one schema-validated UI document reloads in a prebuilt native
   Viewer without a Cargo build, preserves compatible runtime state, produces
   deterministic AI handoff data and enters the release path without shipping
   development-only watcher or preview dependencies.
7. Performance: four independent equal-complexity release workloads report all
   required startup, memory and CPU phases, retain real-window screenshots and
   keep formal application costs separate from Viewer development tooling.

`scripts/check-desktop-proof.ps1 -RequireComplete` is the final platform gate.
Placeholders, declarations, mocks and cross-compilation alone cannot satisfy it.

## Current Baseline

Desktop first-frame setup now keeps Win32, AppKit and Linux windows hidden until
their initial draw content and typed routes are installed. Stateful surface-only
relayout reuses the existing View tree instead of invoking the application view
function again. The Windows Button baseline uses WinUI control height, minimum
width, padding, radius, centered text and semantic border values, backed by
layout/paint/interaction tests and a real Win32 screenshot smoke.

| Area | Status | Meaning |
| --- | --- | --- |
| Rust-first core/runtime | In progress | Typed messages, view runtime, errors, units and feature manifests exist; effects and accessibility remain incomplete |
| Layout/widgets | In progress | The catalog contains 48 component families and all have first-pass runtime surfaces, including optional typed Grid, retained Canvas and Image surfaces, arbitrary-content Flyout, BreadcrumbBar, PasswordBox, ToolTip, ContentDialog, Toast, targeted TeachingTip, InfoBar, ProgressRing, AutoSuggestBox, TreeView, GridView, DataGrid, ToggleButton, NumberBox, Tabs, TimePicker, ColorPicker and CommandPalette controls. Flyout keeps application-owned open state and content while platform profiles select native placement and composition. Canvas uses local Dp geometry, semantic drawing roles, balanced clipping and typed pointer/keyboard activation without exposing renderer handles. Embedded browser controls are intentionally outside v0.2 |
| Rendering/input | Three native renderers with first-pass typed input, focus visuals, IME, modal/targeted overlays, transient timers and native resize proof | Win32 buffered paint, AppKit NSView and Linux Cairo/Pango consume the shared draw plan. All three route click, scroll, focus, keyboard activation, committed text, provisional IME preedit, modal focus trapping and framework-driven animation ticks. Real host resize requests must produce a platform resize callback, exact post-relayout geometry and a final platform-surface capture. AppKit, Linux X11 and real Weston Wayland target runs pass fixed `800x520` and `1180x640` resize scenes, typed Flyout action/focus evidence and Tabs pointer/Right-arrow platform-semantics evidence; broader per-control, IME and accessibility interaction matrices remain required |
| Native services | In progress | Win32 and AppKit own native menu surfaces. The lightweight Linux host owns an in-window desktop menu surface and routes declared accelerators. `NativeFileDialogService` selects Win32 common dialogs, AppKit panels or XDG desktop portals. `NativeDesktopDialogService` maps one `NativeDialogSpec` to owner-bound `MessageBoxW`, `NSAlert`, GTK 4.10 `AlertDialog`, or the desktop-provided Zenity surface without exposing native handles; interaction evidence and localized non-Windows button labels remain release gates. UTF-8 text clipboard dispatch remains behind the optional `clipboard` feature |
| Windows Win32 | First pass | Real host and buffered drawing path exist; every queued GDI text/icon batch is flushed before entering GDI+ so later antialiased fills cannot erase earlier glyphs in the same final frame; typed Grid layout/click plus BreadcrumbBar, PasswordBox, ToolTip, ContentDialog, TeachingTip, ProgressRing, AutoSuggestBox, TreeView, GridView, DataGrid, ToggleButton, NumberBox, Tabs and TimePicker paths have target smoke artifacts. BreadcrumbBar uses application-owned strong IDs, one Tab stop, width-aware ellipsis overflow and typed focus/expanded/selection diagnostics without a child HWND. TeachingTip resolves stable target bounds, draws its tail with GDI `Polygon`, flips/clamps in the viewport and reports semantic focus/response counters. ContentDialog keeps the buffered self-draw path, traps focus in one modal scope and reports semantic focus/response counters without a child HWND. ProgressRing uses antialiased GDI+ arcs and the existing background timer without a native child control. AutoSuggestBox, TreeView, GridView and DataGrid keep application-owned strong IDs and separate typed interaction diagnostics. PasswordBox press-and-hold Peek is self-drawn and its artifacts contain no secret text; accessibility proof remains pending |
| macOS AppKit | Native host/render/input first pass with target proof | Unified run/smoke enter NSApplication with an owned NSView/NSTextInputClient routing resize relayout, mouse, scroll, focus, keyboard, marked text, UTF-8 commits and one owned runtime timer for transient overlays/animations. Fixed Gallery and Notepad runs capture the final NSView bitmap and structured lifecycle/layout/focus/event evidence. Gallery resize proof calls `setContentSize:`, observes `windowDidResize:` and validates exact final geometry before capture. Tabs proof clicks Advanced and verifies the AppKit Right-arrow rule selects About through a second typed message; broader per-control interaction, real IME candidate-window and VoiceOver validation remain incomplete |
| Linux direct | Native host/render/input first pass with X11/Wayland target proof | Unified run/smoke enter real Wayland/X11 event loops, directly present a software surface, use Cairo/Pango and built-in semantic icons, and route resize, pointer, keyboard, focus, preedit and UTF-8 commit into the shared runtime. XDG portal dialogs, an owned menu surface and optional AccessKit-to-AT-SPI projection are connected. Fixed X11 and Weston Wayland runs request real window sizes, observe `WindowEvent::Resized`, and capture exact post-relayout final surfaces with structured interaction evidence. Tabs proof clicks Advanced and verifies Right moves focus to About without changing selection; broader desktop-environment, IME and assistive-technology validation remain incomplete |
| AI/context tooling | First pass | Context packs and validation exist; they must track every new ownership boundary |
| Performance matrix | Windows release baseline established | Minimal, Common, Full Native App and Viewer are independent builds compared against egui, Iced, Slint and Tauri at equal workload size. The matrix records executable size, startup, first frame, empty/full/hidden/peak/private resident memory, idle/repaint CPU, process trees and screenshots; AppKit/Linux target matrices remain future evidence rather than inferred results |
| Reloadable UI authoring | Schema, validator, native Viewer, compatible state reload, value actions, deterministic AI handoff, release embedding and target proof first pass | The optional `ui-document` feature owns schema version 1, stable semantic node IDs, typed layout/theme/localization/accessibility fields, `UiBindingManifest<State, Msg>`, `zsui-uic check`, `handoff` and `embed`. The separate `ui-viewer` feature polls source changes without restarting Cargo, keeps the last valid document after invalid edits, retains ordinary Viewer state and maps node IDs into a reserved stable `WidgetId` namespace. Accepted reloads emit a deterministic preserved/added/reset report. The native input runtime retains focus, text selection and editor viewport for compatible stable IDs, and clears focus/selection/drag/IME state when a node disappears or changes control class. Thirty-two document-ready components now include controlled BreadcrumbBar, AutoSuggestBox, CommandPalette, TreeView, GridView, DatePicker, TimePicker, ColorPicker, PasswordBox, Scroll, NumberBox, ComboBox, List, Tabs, Grid and ProgressRing contracts. BreadcrumbBar uses a typed root-to-current item array with stable semantic string IDs; selection returns the author ID and Boolean overflow state has an explicit property/action loop without exposing the private numeric runtime mapping. AutoSuggestBox uses a typed suggestion array with stable semantic string IDs; query, nullable highlight and expanded state have explicit property/action loops, while structured submission carries both query and optional chosen ID without exposing the private numeric runtime mapping. CommandPalette uses a typed command array with stable semantic string IDs, platform icons and enablement; query, nullable highlight and open state have explicit property/action loops, while invoke returns the author ID and never executes a product command. TreeView uses a recursive typed node array with globally unique semantic string IDs, a complete expanded-ID set and nullable selection; selection and invocation emit one stable ID, expansion emits the full next set, and the release runtime rejects unknown or non-expandable references without using hierarchy paths as identity. GridView uses a typed tile array with unique semantic string IDs, titles, optional subtitles and semantic icons; nullable selection, selection and invocation remain explicit, while the release runtime rejects collisions or unknown selections without using responsive column position as identity. DatePicker uses canonical ISO `date` bindings plus typed `ZsDate` manifest helpers; selected date, visible month and expanded state each have explicit property/action loops, with static and resolved range validation. TimePicker uses canonical `HH:MM` `time` bindings plus typed `ZsTime` manifest helpers; selected time and expanded state have explicit property/action loops, while minute increments and clock-format policy are validated without serializing platform display text. ColorPicker uses canonical uppercase `#RRGGBBAA` `color` bindings plus typed `Color` manifest helpers; selected color, expanded state and active channel have independent property/action loops, while alpha policy is validated without replacing platform-owned picker metrics. PasswordBox uses non-serializable `ZsPassword`/`UiSecretValues` state and a separate typed action channel; document literals and JSON values are rejected, while Viewer logs and AI handoff retain redacted metadata only. List uses each direct child's stable `UiNodeId` as its controlled string selection value, so declaration reordering does not change item identity. ProgressRing uses a nullable numeric value for determinate or indeterminate mode and resolves platform-owned size metrics. Tabs uses each direct child's stable `UiNodeId` as a typed content-slot identity, validates complete label maps and optional semantic icons, and preserves the selected slot through one string property/action binding. Grid uses typed fixed/fraction tracks and a complete placement map keyed by stable direct-child IDs, so sibling declaration order cannot silently move content; validation and release compilation reject invalid weights, spans, gaps, references and track bounds. UiDocument spacing tokens resolve through the target platform profile; Text exposes validated semantic role, wrapping, ellipsis, weight and alignment. Recursive intrinsic layout includes descendant line boxes, gaps, padding and native control minimums, and horizontal rows measure wrapped text at its actual allocated width. Text, toggle, slider, NumberBox, ComboBox, BreadcrumbBar, AutoSuggestBox, CommandPalette, TreeView, GridView, DatePicker, TimePicker, ColorPicker, List, Tabs and scroll actions carry schema-validated values through per-control typed callbacks, update explicitly bound Viewer state and rebuild without a global registry. Layout clamps restored scroll offsets to the current content range. Viewer smoke can inject real native scroll and repeatable click sequences at fixed points and fails unless the host reports typed routing; Windows AutoSuggestBox proof submits a stable suggestion through one real click and records four Viewer messages with zero unhandled clicks, CommandPalette proof invokes one stable command and closes its controlled overlay through one real click with two Viewer messages and zero unhandled clicks, TreeView proof selects and invokes one stable row through one real click with two Viewer messages and zero unhandled clicks, GridView proof selects and invokes one stable tile through one real click with two Viewer messages and zero unhandled clicks, DatePicker proof changes July to August through a real click, TimePicker proof changes `09:30` to `10:45` through two real clicks, and ColorPicker proof changes the active channel and canonical RGBA value through one real click before retaining the final-window PNG. Its smoke mode emits the final native window PNG plus structured runtime, typography and memory evidence. The handoff command canonicalizes the document, binding schema, optional value snapshot and optional final native PNG into a stable manifest. The feature-pruned `ui-document-runtime` owns the reusable document-to-`ViewNode<Msg>` compiler; it enables no widget family itself. `embed` emits a deterministic versioned artifact whose decode path rechecks payload fingerprint, document schema, application binding schema and compiled component features without linking Viewer, watchers, previews or an extra process. Complete component/property coverage and broader advanced-control state migration remain required; a full visual designer is not part of this gate |
| Reloadable Viewer target proof | Fixed AppKit and Linux target proof passes | Native UI Proof run `29883039068` for commit `348808b6f5b862d90c19d8687a15f991e8790344` passes both target jobs. `zsui.ui-viewer-proof/v1` records target capture identity, logical/pixel window metrics and deterministic 15-node layout snapshots. AppKit and Linux Direct build the same precompiled Viewer, load the same `scrolling.json`, handle one native scroll, emit one typed Viewer message and retain the final `NSView` or Linux presented-surface PNG. The hosted-run evidence measured about 61.83 MiB on AppKit and 27.26 MiB on Linux; these are run-specific resident-memory observations, not universal benchmarks |
| Current UiDocument schema count | 33 document-ready components | The schema and release compiler include Flyout with a page-only stable target, arbitrary View content, positive geometry, typed dismissal reasons and controlled open state; BreadcrumbBar, TeachingTip and Tooltip retain their navigation, targeted and attached overlay contracts. |
| Three-platform proof | First blocking suite passes | Win32 local proof plus fixed GitHub-hosted AppKit, X11 and Weston Wayland jobs produce final-surface PNGs and structured lifecycle/layout/focus/event reports. The v0.2 gate remains open until the required services, IME, accessibility and broader interaction matrix are complete |

## Execution Order

Work proceeds in complete vertical slices, not by declaring many empty widgets:

1. Keep core runtime, draw plan, event and capability contracts stable.
2. Finish one high-frequency application path on Win32 without visual regressions.
3. Carry the same path through real AppKit and Linux adapters.
4. Add unit, feature-matrix, target compile and runtime evidence.
5. Update the component catalog and completion percentage only after evidence.
6. Continue with the next highest-value path: text/IME, focus, scroll/list,
   menu/command, theme/icon, dialogs, accessibility, then additional widgets.
7. Complete the reloadable authoring path without weakening those native
   gates: expand the schema and native Viewer beyond the current document-ready
   set and extend compatible state retention to advanced controls.
   Defer the full visual editor.

ZSUI v0.2 completes only when one shared nontrivial application is genuinely
usable on all three native backends and the framework layers above are verified
as a coherent whole, including the minimum reloadable UI-document authoring
gate. A native window, a large component catalog, a polished Windows-only demo
or a browser-only mock preview cannot complete the milestone by itself.
